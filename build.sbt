enablePlugins(AssetUploaderPlugin)

useAssetRepository := true

compileAssetsBeforeUpload := false // (optional) default: true, set this if you want to manually trigger asset compilation for uploads

assetRepoName := "master"
